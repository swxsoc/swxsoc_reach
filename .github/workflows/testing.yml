# This workflow will install Python dependencies and run tests on expected user platforms
name: Testing on user machines

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch: # For on demand runs
  schedule:
    - cron: 0 0 * * * # Scheduled run every day at midnight

jobs:
  build:
    #
    runs-on: ${{ matrix.platform }}
    #
    strategy:
      fail-fast: false
      matrix:
        platform: [ubuntu-latest, macos-latest, windows-latest]
        python-version: ['3.10', '3.11', '3.12', '3.13']

    steps:
    - uses: actions/checkout@v4
      #
    - name: Set up Python ${{ matrix.python-version }}
      #
      uses: actions/setup-python@v5
      with:
        #
        python-version: ${{ matrix.python-version }}
        #
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip setuptools wheel
        python -m pip install -e '.[test]'


    - name: Run tests
      run: pytest --pyargs swxsoc_reach --cov --junitxml=junit.xml -o junit_family=legacy swxsoc_reach
      env:
        #
        PLATFORM: ${{ matrix.platform }}
        #
    - name: Upload test results to Codecov
      if: ${{ !cancelled() }}
      uses: codecov/test-results-action@v1
      with:
        token: ${{ secrets.CODECOV_TOKEN }}

